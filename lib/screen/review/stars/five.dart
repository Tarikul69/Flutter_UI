import 'package:flutter/material.dart';

class five extends StatelessWidget {
  const five({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Five Stars"),
      ),
    );
  }
}
