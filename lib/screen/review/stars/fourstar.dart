import 'package:flutter/material.dart';

class fourstar extends StatelessWidget {
  const fourstar({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Four Star"),
      ),
    );
  }
}
