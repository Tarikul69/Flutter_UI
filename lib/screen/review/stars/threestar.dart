import 'package:flutter/material.dart';

class threestar extends StatelessWidget {
  const threestar({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Three star"),
      ),
    );
  }
}
